// const errorHandler = (err, req, res, next) => { // esse middleware é um middleware de tratamento de erros
//     console.error(err.stack);  // Loga o erro no console
//     res.status(500).json({ message: 'Algo deu errado.', error: err.message }); // Envia uma resposta de erro
// };

// export default errorHandler;